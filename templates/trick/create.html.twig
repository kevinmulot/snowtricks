{% extends 'base.html.twig' %}

{% block title %}Tricks{% endblock %}

{% block body %}
    <div class="container">
        <h1>Create</h1>
        <ul>{{ form_start(trickForm) }}
            {{ form_row(trickForm.name) }}
            {{ form_row(trickForm.content) }}
            {{ form_row(trickForm.category) }}
            {{ form_row(trickForm.submit) }}
            {{ form_end(trickForm) }}
        </ul>
    </div>
{% endblock %}
