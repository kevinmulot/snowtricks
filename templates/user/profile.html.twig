{% extends 'base.html.twig' %}

{% block title %}Hello{% endblock %}

{% block body %}

    <div class="container">
        <h1>Edit profile</h1>
        <ul>{{ form_start(profileForm) }}
            {{ form_row(profileForm.imageFile) }}
            {{ form_row(profileForm.submit) }}
            {{ form_end(profileForm) }}
        </ul>
    </div>
{% endblock %}
