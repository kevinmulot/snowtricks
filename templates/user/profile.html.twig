{% extends 'base.html.twig' %}

{% block title %}Profile{% endblock %}

{% block body %}
    <div class="container">
        <div class="row justify-content-center">
            <h1 class="h1 col-12 mt-3 mb-3 font-weight-normal text-center d-md-block d-none">Edit your picture</h1>
            <div class="border text-center mt-4 col-8">
                <h2 class="h1 mb-3 font-weight-normal text-center d-none d-sm-block d-block d-md-none">Edit your
                    picture</h2>
                {{ form_start(profileForm) }}
                {{ form_row(profileForm.imageFile) }}
                {{ form_row(profileForm.submit) }}
                {{ form_end(profileForm) }}
            </div>
        </div>
    </div>
{% endblock %}
